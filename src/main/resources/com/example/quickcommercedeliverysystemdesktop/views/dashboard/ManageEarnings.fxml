<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<VBox xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.quickcommercedeliverysystemdesktop.controllers.dashboard.ManageEarningsController"
      spacing="20" styleClass="admin-container">
    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Header -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Text text="💵 Manage Earnings" styleClass="page-title"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="📥 Export CSV" onAction="#handleExportCSV" styleClass="btn-primary"
                style="-fx-background-color: #27ae60;"/>
        <Button text="🔄 Refresh" onAction="#handleRefresh" styleClass="btn-primary"/>
    </HBox>

    <!-- Total Earnings Card -->
    <HBox spacing="15">
        <VBox styleClass="stat-card stat-card-green" HBox.hgrow="ALWAYS">
            <padding>
                <Insets top="20" right="20" bottom="10" left="20"/>
            </padding>
            <Label text="Total System Earnings" styleClass="stat-card-title"
                   style="-fx-font-size: 16px;"/>
            <Label fx:id="totalEarningsLabel" text="0.00" styleClass="stat-card-value"
                   style="-fx-font-size: 42px; -fx-text-fill: #27ae60;"/>
            <Label fx:id="earningsCountLabel" text="0 transactions" styleClass="stat-card-subtitle"/>
        </VBox>
    </HBox>

    <!-- Filter Section -->
    <VBox spacing="10" styleClass="filter-section">
        <padding>
            <Insets top="15" right="20" bottom="15" left="20"/>
        </padding>

        <!-- Filter Labels Row -->
        <HBox spacing="15" alignment="CENTER_LEFT">
            <Label text="Delivery Person:" styleClass="filter-label" prefWidth="250"/>
            <Label text="From Date:" styleClass="filter-label" prefWidth="180"/>
            <Label text="To Date:" styleClass="filter-label" prefWidth="180"/>
            <Region HBox.hgrow="ALWAYS"/>
        </HBox>

        <!-- Filter Controls Row -->
        <HBox spacing="15" alignment="CENTER_LEFT">
            <ComboBox fx:id="deliveryPersonComboBox" prefWidth="250" styleClass="filter-combo"/>
            <DatePicker fx:id="fromDatePicker" prefWidth="180"/>
            <DatePicker fx:id="toDatePicker" prefWidth="180"/>
            <Button text="Filter" onAction="#handleApplyFilters" styleClass="btn-primary" prefWidth="100"/>
            <Button text="Clear" onAction="#handleClearFilters" styleClass="secondary-button" prefWidth="80"/>
        </HBox>
    </VBox>

    <!-- Earnings Table -->
    <VBox VBox.vgrow="ALWAYS" styleClass="content-card">
        <padding>
            <Insets top="15" right="15" bottom="15" left="15"/>
        </padding>

        <TableView fx:id="earningsTable" VBox.vgrow="ALWAYS" styleClass="orders-table">
            <columns>
                <TableColumn fx:id="earningIdColumn" text="ID" prefWidth="60" minWidth="50"/>
                <TableColumn fx:id="orderIdColumn" text="Order" prefWidth="80" minWidth="70"/>
                <TableColumn fx:id="deliveryPersonColumn" text="Delivery Person" prefWidth="200" minWidth="150"/>
                <TableColumn fx:id="customerColumn" text="Customer" prefWidth="180" minWidth="130"/>
                <TableColumn fx:id="productColumn" text="Product" prefWidth="220" minWidth="150"/>
                <TableColumn fx:id="amountColumn" text="Amount" prefWidth="110" minWidth="90"/>
                <TableColumn fx:id="dateColumn" text="Date &amp; Time" prefWidth="180" minWidth="150"/>
            </columns>
            <placeholder>
                <Label text="No earnings found" styleClass="placeholder-label"/>
            </placeholder>
        </TableView>
    </VBox>

    <!-- Help Text -->
    <HBox spacing="10" alignment="CENTER">
        <Label text="💡 Tip: Use filters to view earnings by specific delivery person or date range. Export to CSV for reporting."
               styleClass="help-text" wrapText="true"/>
    </HBox>
</VBox>

